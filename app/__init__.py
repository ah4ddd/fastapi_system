# An __init__.py file (can be completely empty)
# tells Python "this directory is a package, allow imports from it."
# Without it, Python treats the directory as just a folder,
# not something you can import from.
